<h1>all </h1>
<table>
  <thead>
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th>Edit</th>
    <th>Delete</th>
  </tr>
  </thead>

  <tbody>
  <% @typeFuels.each do |typeFuel| %>
    <tr>
      <td><%= typeFuel.id %></td>
      <td><%= typeFuel.name %></td>
      <td><%= link_to 'Редагувати', type_fuels_edit_path+"?id="+typeFuel.id.to_s, class: "link2" %></td>
      <td><%= link_to 'Видалити',type_fuels_delete_path+"?id="+typeFuel.id.to_s, method: :delete, data: { turbo_method: "delete" }, class: "link2" %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>
<%= link_to "Новий статус", type_fuels_new_path, class: "link" %>